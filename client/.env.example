# Auth0 Configuration for Next.js Client
# These must be prefixed with NEXT_PUBLIC_ to be available in the browser

# Domain from your Auth0 application settings
NEXT_PUBLIC_AUTH0_DOMAIN=your-auth0-domain.us.auth0.com

# Client ID from your Auth0 application settings
NEXT_PUBLIC_AUTH0_CLIENT_ID=your_auth0_client_id

# Redirect URI after authentication (must be configured in Auth0 as an allowed callback URL)
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXT_PUBLIC_AUTH0_REDIRECT_URI=http://localhost:3000

# API Audience from your Auth0 API settings (must match server configuration)
NEXT_PUBLIC_AUTH0_AUDIENCE=your-api-audience

# Optional: Direct server URL (if not using nginx proxy)
# For local development connecting to local server: http://localhost:8002
# Leave unset to use the default /api proxy (recommended for Docker deployments)
# NEXT_PUBLIC_SERVER_URL=http://localhost:8002
