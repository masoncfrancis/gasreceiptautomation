# LubeLogger Configuration
# URL of your LubeLogger instance (required)
# Example: http://localhost:8080 or http://lubelogger:8080 (when using Docker Compose)
LUBELOGGER_URL=http://localhost:8080

# Google Gemini API Configuration
# Get your API key from: https://ai.google.dev/gemini-api/docs/api-key
GEMINI_API_KEY=your_gemini_api_key_here

# Auth0 Configuration
# Domain from your Auth0 application settings
AUTH0_DOMAIN=your-auth0-domain.us.auth0.com

# API Audience from your Auth0 API settings
AUTH0_API_AUDIENCE=your-api-audience

# Issuer URL (typically https://your-domain.us.auth0.com/)
AUTH0_ISSUER=https://your-auth0-domain.us.auth0.com/

# Algorithm used for JWT token validation (default: RS256)
AUTH0_ALGORITHMS=RS256
